# Configuración Nixpacks para Railway
# Railway detecta el proyecto Java y usa este archivo para el build

[phases.build]
cmds = ["./mvnw package -DskipTests -q"]

[start]
cmd = "java -jar target/matchup-backend-0.0.1-SNAPSHOT.jar"
