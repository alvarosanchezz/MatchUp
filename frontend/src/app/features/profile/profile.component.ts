import { Component } from '@angular/core';

@Component({
  selector: 'app-profile',
  standalone: true,
  template: `
    <div class="flex items-center justify-center h-64">
      <p class="text-gray-500 text-lg">Perfil — próximamente</p>
    </div>
  `,
})
export class ProfileComponent {}
