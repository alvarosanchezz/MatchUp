import { Component } from '@angular/core';

@Component({
  selector: 'app-user-profile',
  standalone: true,
  template: `
    <div class="flex items-center justify-center h-64">
      <p class="text-gray-500 text-lg">Perfil público — próximamente</p>
    </div>
  `,
})
export class UserProfileComponent {}
